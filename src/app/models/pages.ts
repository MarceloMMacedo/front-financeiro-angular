export interface Pages {
    content?: [],
    pageable?: {},
    totalPages?: number,
    totalElements?: number,
    last?: boolean,
    size?: number,
    numberOfElements?: number,
    sort?: { },
    number?: number,
    first?: boolean,
    empty?: boolean
}
